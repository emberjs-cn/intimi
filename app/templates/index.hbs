<div class="messages">
  <div {{bindAttr class=':alert mobileAccountAlertLevelUp:alert-error'}}>
    {{#if Intimi.Auth.user.mobileAccount}}
      {{#if Intimi.Auth.user.mobileAccount.needRecharge}}
        {{!#TODO 处理支付宝返回数据}}
        你的账户余额只有<strong>{{Intimi.Auth.user.mobileAccount.balance}}</strong>元, 是否<a data-toggle="modal" href='#selectPrepaidCardModal'>立即充值</a>？
      {{else}}
        你的账户余额为{{Intimi.Auth.user.mobileAccount.balance}}元。
      {{/if}}
    {{else}}
      <strong>抱歉! </strong>您还未开通即时通知服务, 开通后即可使用短信、彩信与您的好友取得联系。是否现在<a {{action 'registerMobileAccount'}}>开通服务</a>？
    {{/if}}
  </div>
</div>

{{view Intimi.SelectPrepaidCardPopupView}}
