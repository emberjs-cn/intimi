{{#if Intimi.Auth.signedIn}}
  {{#view InkyLike.AppView titleBinding="title" sidebarBinding="sidebar"}}
    {{outlet}}

    {{#if newMessagePopupVisible}}
      {{view Intimi.NewMessagePopupView}}
    {{/if}}
  {{/view}}
{{else}}
  {{view Intimi.AuthSignInView}}
{{/if}}
