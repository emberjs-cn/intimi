{{#if Intimi.Auth.user}}
  {{#view InkyLike.AppView titleBinding="title" sidebarBinding="availableSidebar"}}
    {{outlet}}

    {{#if newMessagePopupVisible}}
      {{view Intimi.NewMessagePopupView}}
    {{/if}}
  {{/view}}
{{else}}
  {{view Intimi.AuthSignInView}}
{{/if}}
